#!/bin/sh
#
# mkcd -- create a directory (and parents) then cd into it.
#
# Usage: mkcd <dir>
#
mkcd () {
    if [ $# -eq 0 ]; then
        echo "Usage: mkcd <dir>" >&2
        return 1
    fi
    mkdir -p -- "$1" && cd -- "$1" || return 1
}
